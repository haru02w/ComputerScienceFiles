/*
 * 26. Compile o programa abaixo e explique as saídas obtidas.
 */
#include <stdio.h>
int main() {
  printf("sizeof (unsigned): %lu\n", sizeof(unsigned));
  printf("sizeof (int) = %lu\n", sizeof(int));
  return 0;
}
/*
 * O programa apresenta o tamanho em bytes dos tipos "unsigned int" e "int"
 */
